import { PlaygroundExperience } from "@/components/playground/playground-experience";

export const metadata = {
  title: "Coding Playground | AIMLverse"
};

export default function PlaygroundPage() {
  return <PlaygroundExperience />;
}
